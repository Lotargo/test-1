# Decomposition

1. Keep a single tiny Python entry point in `hello_world.py`.
2. Use TODO items that are small, deterministic, and easy to validate.
3. After provider completion, validate the repository with Python syntax checks.
4. Only mark TODO items complete after validation succeeds.
